kubernetesClusterDomain: cluster.local

project_id: pasvel-moj-t-ocr
test:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  replicas: 1
  test:
    image:
      repository: me-west1-docker.pkg.dev/pasvel-moj-d-ocr/ocr-t-docker/test
      tag: latest
  type: LoadBalancer
