kubernetesClusterDomain: cluster.local
nginx:
  nginx:
    image:
      repository: me-west1-docker.pkg.dev/pasvel-moj-d-ocr/ocr-d-docker/test
      tag: latest
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  replicas: 1
  type: LoadBalancer
ocrDefault:
  serviceAccount:
    annotations:
      iam.gke.io/gcp-service-account: ocr-default@pasvel-moj-d-ocr.iam.gserviceaccount.com
